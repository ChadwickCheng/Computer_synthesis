/*
【IPv4快速识别表】- 看第一个点分十进制字节数
A类    | 1-126     | 单播
B类    | 128-191   | 单播（如192是C类错了！）
C类    | 192-223   | 单播（如192-223确实是C类）
D类    | 224-239   | 组播
E类    | 240-255   | 预留

例子：
192.x.x.x → 192在192-223范围 → C类 ✓
172.x.x.x → 172在128-191范围 → B类 ✓
10.x.x.x  → 10在1-126范围   → A类 ✓

1. IP地址：
    逻辑地址，NAT实现私-公
    IPv4：32位，点分十进制，4字节，4段，每段0-255
    组成：网络号+主机号，网络号为所属的物理网络编号，主机号为该物理网络上主机的编号
    编址经历：分类-子网-无分类
    A类：最大；B类：中等；C类：最小，最多254台主机
    分配原则：1. 只能分配ABC类地址 2. 主机号全0是网络地址，全1是广播地址，二者不可分配
2. A类：
    1. 网络号8位，0开头；主机号24位
    2. 最小的网络号全0不分配；最大网络号127，本机回环不分配；
    3，私有地址：10.x.x.x，用于局域网
3. B类：
    1. 网络号16位，10开头；主机号16位
    2. 保留地址：169.254.x.x,DHCP失败自动分配
    3. 私有地址：172.16.x.x - 172.31.x.x
4. C类：
    1. 网络号24位，110开头；主机号8位
    2. 私有地址：192.168.x.x
5. D类：组播
    计算机网络通信模式：单播，广播，组播(主机-组内主机)
    224.0.0.0 - 239.255.255.255
    224.0.0.1 本地网络所有主机 224.0.0.2 本地网络所有路由器
    组播地址仅用于目标地址
6. 子网掩码：将主机号进一步划分为子网号和主机号
    1. 对应ip的网络号和子网号，子网掩码全为1
    2. 与运算判断是否在同一子网
    3. 缺省子网掩码，未划分子网，比如C类为255.255.255.0
    4. 自定义子网掩码
7. CIDR无分类编址：
    1. 两级编址(网络+主机)
    2. 斜线表示法：/后数字表示网络号位数，如192.168.1.0/24表示网络号24位
    3. 聚合C类网络数量计算：
       例子：138.16.35.7/20 聚合了多少个C类网络？
       
       【计算公式】聚合C类数 = 2^(24-网络位数) 
       或者：2^主机位数 / 2^8
       
       【解析】
       - /20 表示网络前缀20位，主机位 = 32-20 = 12位
       - 该网络总地址数 = 2^12 = 4096个地址
       - C类网络是/24，每个C类地址数 = 2^8 = 256个地址
       - 聚合C类数 = 2^12 / 2^8 = 2^4 = 16个C类网络
       
       或直接算：24-20 = 4，所以 2^4 = 16个
       
       记住：网络前缀越小(/20 < /24)，网络越大，包含的C类网络越多
    4. 应用：路由汇聚，找最长相同前缀得到网络号，主机位全取0得到ip地址

*/